<Page x:Class="RemoteSystemWpf.Pages.SelectionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SelectionPage"
      Background="Transparent">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/RemoteSystemWpf;component/Images/DefaultBackground.jpg" 
                Stretch="UniformToFill"/>
        </Grid.Background>

        <Rectangle Fill="#60000000"/>

        <StackPanel x:Name="ChoicerPanel" VerticalAlignment="Center">
            <Label x:Name="MainLabel"
           Content="Выберите тип доступа"
           HorizontalAlignment="Center"
           FontSize="40"
           Foreground="#F5DEB3"
           Margin="0,0,0,40"/>

            <Grid x:Name="Choicer">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                Content="Сервер"
                Click="Server"
                Height="100" Width="220"
                FontSize="40"
                Background="#F5DEB3"
                FontWeight="SemiBold"
                HorizontalAlignment="Center"/>

                <Button Grid.Column="1"
                Content="Клиент"
                Click="Client"
                Height="100" Width="220"
                FontSize="40"
                Background="#F5DEB3"
                FontWeight="SemiBold"
                HorizontalAlignment="Center"/>
            </Grid>
        </StackPanel>
    </Grid>
</Page>