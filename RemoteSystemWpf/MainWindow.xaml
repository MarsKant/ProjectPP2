<Window x:Class="RemoteSystemWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RemoteSystemWpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="pack://application:,,,/RemoteSystemWpf;component/Images/DefaultBackground.jpg" 
                Stretch="UniformToFill"/>
        </Grid.Background>

        <Rectangle Fill="#40000000"/>
        <Frame x:Name="frame" NavigationUIVisibility="Hidden"/>
        <Label 
            x:Name="MainLabel"
            Content="Выберите тип доступа"
            HorizontalAlignment="Center"
            FontSize="40"
            Foreground="#F5DEB3"/>
        <Grid x:Name="Choicer">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button
                Grid.Column="0"
                Content="Сервер"
                Click="Server"
                Height="100"
                Width="200"
                FontSize="40"
                Background="#F5DEB3"
                FontWeight="SemiBold"
                HorizontalAlignment="Center"/>
            <Button
                Grid.Column="1"
                Content="Клиент"
                Click="Client"
                Height="100"
                Width="200"
                FontSize="40"
                Background="#F5DEB3"
                FontWeight="SemiBold"
                HorizontalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
